PRISMTRACE_API_KEY=
PRISMTRACE_PROJECT_ID=
PRISMTRACE_HOST=

# Models (Ollama). The assistant reuses the generator model when left empty.
# ASSISTANT_MODEL=llama3.1:latest
# GENERATOR_MODEL=qwen2.5:3b
# VERIFIER_MODEL=llama3.2:3b

# Auth (HMAC-signed JWTs). Override in any shared/prod deployment.
JWT_SECRET=change-me-to-a-long-random-string